type: object
properties:
  data:
    type: object
    properties:
      entered_data:
        $ref: '../objects/expenses.yaml'
      external_id:
        type: string
        description: |
          The external id generated after creating the expense.
required: [ data ]